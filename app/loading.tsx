import SkeletionLoader from '@/components/layout/skeletion-loader';
import React from 'react';

const loading = () => {
    return (
        <div>
            <SkeletionLoader />
        </div>
    );
};

export default loading;
